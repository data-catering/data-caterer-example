CREATE TABLE IF NOT EXISTS data_caterer_test.accounts (
    account_number STRING,
    amount FLOAT64,
    created_by STRING,
    created_by_fixed_length STRING,
    open_timestamp TIMESTAMP,
    account_status STRING
);
